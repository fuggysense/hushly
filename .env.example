# Client (bundled into app)
EXPO_PUBLIC_API_BASE=https://hushly.genflos.com

# Server-only (API routes / VPS — never bundled)
DATABASE_URL=postgres://hushly:password@localhost:5432/hushly
DATABASE_SSL=false
DEEPGRAM_API_KEY=
OPENAI_API_KEY=
CLEANUP_PROVIDER=openai
CLEANUP_MODEL=gpt-5-nano
HUSHLY_MASTER_KEY=replace-with-a-long-random-admin-password
